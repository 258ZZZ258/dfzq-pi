node_modules
dist/
.artifacts/
*.log
.DS_Store
*.tsbuildinfo
# packages/*/node_modules/
packages/*/dist/
packages/*/dist-chrome/
packages/*/dist-firefox/
# packages/ai/src/providers/data/   ← 内网分支刻意取消忽略:该目录由 hydrate:model-data
#   联网生成,离线内网无法重建,而 packages/ai 的测试会 import ./data/*.json。
#   548K / 37 个文件,直接进版本库换 CI 零联网依赖。从 origin 同步时这一行会冲突,
#   保留本地(注释)版本即可。
packages/ai/src/providers/.model-generation-*/
*.cpuprofile

# Environment
.env
.runtime/

# Editor files
.vscode/
.zed/
.idea/
.claude/
*.swp
*.swo
*~

# Package specific
.npm/
coverage/
.nyc_output/
__pycache__/
.pi_config/
tui-debug.log
compaction-results/
.opencode/
syntax.jsonl
out.jsonl
pi-*.html
out.html
packages/task-runtime/output/audit-report-*/
packages/coding-agent/binaries/
todo.md
plans/
.pi/hf-sessions/
.pi/hf-sessions-backup/
collect.sh

# dfzq 开发文档(含客户信息与内网架构决策,不进版本库)
dfzq-pi开发任务/

# SDD 执行台账与中间产物(含审查 diff、任务报告、评测运行产物)。
# 原先只在本机 .git/info/exclude 里忽略 —— 那条规则不随克隆走,新克隆就会失去保护。
# 本仓库有三个 remote(其中 fork 是公开仓),客户数据不得入公开仓,故在此做纵深防御。
.superpowers/
