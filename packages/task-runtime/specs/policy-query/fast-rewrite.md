把下面这个问题改写成 1–3 条**监管表述**的检索词,用于制度库检索。

口语化的情景描述直接检索命中率低,要先转成监管用语。
例:「员工发二维码给朋友开户算不算违规」→「证券公司员工 招揽客户 开立账户 展业行为规范」

**只输出一个 JSON 对象,放在 ```json 围栏里,不要任何其他文字。对象只有一个键 `queries`,
值是一个字符串数组(1–3 条检索词)。**

例:
```json
{"queries": ["证券公司员工 招揽客户 展业行为规范", "证券经纪业务 客户开户 合规要求"]}
```

问题:
